# Environment Configuration
# Use "development" for local dev (enables MCP/Picnic), "production" for deployment (disables MCP)
ENVIRONMENT=production

# CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173  # Replace with your frontend origins
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOW_HEADERS=Content-Type,Authorization,X-Requested-With

# LLMs
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# MCP/Picnic Integration (only needed for local development)
# PICNIC_USERNAME=your-email@example.com
# PICNIC_PASSWORD=your-password